# One environment for the whole pipeline. From this folder:
#
#   uv venv --python 3.13
#   uv pip install -r requirements.txt
#
# Versions are the ones the test suites pass with.

# --- the pipeline's own code, installed editable so edits apply immediately ---
-e ./core          # pipeline_core: the shared schema, and the `pipeline-db` command
-e ./01-scrape     # stage1_scrape
-e ./02-enrich     # stage2_enrich
# 03-outreach is plain scripts, not a package -- only its dependencies are listed.

# --- core: database ---
SQLAlchemy==2.0.54

# --- 01-scrape: portal, HTML, PDF text, spreadsheet export ---
beautifulsoup4==4.15.0
lxml==6.1.2
requests==2.34.2
pypdf==6.16.2
pandas==3.0.6
openpyxl==3.1.5

# --- 02-enrich: the model call ---
langchain==1.3.18
langchain-openai==1.6.0
pydantic==2.13.5

# --- 03-outreach: Microsoft Graph sign-in ---
msal==1.39.0

# --- every stage reads its own .env ---
python-dotenv==1.2.3

# --- tests ---
pytest==9.1.1

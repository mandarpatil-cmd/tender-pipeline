# Copy to `.env` (never commit `.env`).
#
# Stage 1 needs one key: OpenRouter, used only to read the 6-character search
# captcha. Optional overrides (OCR model, base URL, attribution headers) are
# listed in README.md.

# https://openrouter.ai/keys
OPENROUTER_API_KEY=
